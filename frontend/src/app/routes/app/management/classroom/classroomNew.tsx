import styles from "@/features/management/style/classroom.module.css";

const ClassroomNew = () => {
  return (
    <div className={styles.contentsContainer}>
      <h2 className={styles.sectionName}>授業ルーム作成</h2>
    </div>
  );
};

export default ClassroomNew;
