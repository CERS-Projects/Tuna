export type categoryType = {
	categoryId: string;
	categoryName: string;
	createdAt: string;
};

export type materialType = {
	documentId: string;
	documentName: string;
	documentUrl: string;
	uploadedAt: string;
};

export type classroomData = {
	roomName: string;
	description: string;
};
